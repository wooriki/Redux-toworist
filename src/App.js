import "./App.css";
import Router from "./Shared/Router";

// 기본적으로 Router를 활용해서 전체적으로 제어해준다.
function App() {
    return (
        <>
            <Router />
        </>
    );
}

export default App;
